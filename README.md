# Personal-Assistant
An AI chatbot web app built with React, HTML, and CSS for a smooth UI. Axios connects the frontend with Firebase, which handles secure login and real-time chat storage. Google Gemini API powers the chatbot to provide fast, intelligent, human-like responses.
